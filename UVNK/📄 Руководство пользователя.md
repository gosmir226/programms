## Программа: Получение технологических параметров из отчетов печей УВНК-9А1 и УВНК-9А2

Эта программа предназначена для автоматической обработки данных из отчетов печей, объединения их с паспортными данными и формирования итогового Excel-файла.

---

## 📦 Установка

Убедитесь, что Python установлен на вашем компьютере.

Для установки необходимых библиотек выполните команду:

```bash
pip install PyQt6 nptdms pandas openpyxl
```

---

## 🚀 Запуск программы

Запустите файл `main_gui.py`.  
Появится графическое окно программы.

---

## 📁 Шаг 1: Выбор директории с входными данными

Нажмите кнопку **"Обзор..."** в поле "Папка с данными".

❗ **ВАЖНО: Структура папок**
Выбирайте **КОРНЕВУЮ папку**, в которой лежат папки с названиями установок (например, `UVNK-9A1`, `UVNK-9A2`).
Внутри каждой папки установки должны быть две папки:
- `Pasport` — с файлами паспортов (`.tdms`)
- `Reports` — с файлами отчетов (`.tdms`)

Пример правильной структуры:
```
📂 Мои_Данные (Выбирать эту папку!)
 ├── 📂 UVNK-9A1
 │    ├── 📂 Pasport
 │    └── 📂 Reports
 ├── 📂 UVNK-9A2
 │    ├── 📂 Pasport
 │    └── 📂 Reports
```

Программа автоматически просканирует все подпапки и обработает данные из них.

---

## 📄 Шаг 2: Укажите файл результата

Нажмите **"Выбрать..."** в поле "Файл результата".
Укажите имя и местоположение Excel-файла, куда будут сохранены данные.

- Если файл не существует, он будет создан.
- Если файл существует, новые данные будут добавлены к нему.

---

## 🔁 Шаг 3: Обработка

Нажмите кнопку **" ЗАПУСТИТЬ ОБРАБОТКУ"**.

В процессе работы в окне "Журнал событий" будет отображаться ход выполнения.

---

## 📊 Результат

В итоговом Excel-файле будет создано два листа:

1. **Data** — Основная таблица с рассчитанными параметрами.
   - Столбец `Установка` указывает, к какой папке относятся данные.
2. **Logs** — Журнал обработки файлов.
   - Содержит информацию о каждом файле: был ли он обработан или пропущен, и по какой причине (например, "Нет паспорта", "Ошибка структуры" и т.д.).

---

## 🧠 Кэширование (cache.json)

Программа запоминает обработанные файлы в `cache.json` рядом с программой.
- Повторно запущенная обработка пропустит уже добавленные файлы, чтобы не дублировать строки.
- Если вы хотите пересчитать все заново: удалите `cache.json` или выберите новый выходной файл.

---

## 🛑 Решение проблем

- **"Пропуск: нет папок Pasport/Reports"**: Проверьте, что внутри папок установок точно есть папки с именами `Pasport` и `Reports`.
- **"Reports не прочитан"**: Файл `.tdms` может быть поврежден или открыт в другой программе.