## Программа: Сортировка файлов по отклонениям (Point Deviation)

Эта программа предназначена для автоматического анализа протоколов измерений (PDF и HTML) и их сортировки на основе величины отклонений точек.

---

## 📦 Установка

Убедитесь, что Python установлен на вашем компьютере.

Для установки необходимых библиотек выполните команду:

```bash
pip install PyQt6 pdfplumber pandas chardet
```

---

## 🚀 Запуск программы

Запустите файл `main_gui.py` из папки `point_deviation` или используйте общий навигатор `navigation.py`.
Появится графическое окно программы.

---

## 📁 Шаг 1: Выбор исходной папки

Нажмите кнопку **"Обзор..."** в поле "Исходная папка".
Выберите папку, в которой находятся файлы протоколов (`.pdf` или `.html`).

- Программа сканирует **все вложенные папки**.
- Для HTML файлов поддерживаются только файлы, содержащие "откл" в названии (например, `отклонения.html`).
- PDF файлы обрабатываются все.

---

## 📂 Шаг 2: Выбор целевой папки

Нажмите кнопку **"Обзор..."** в поле "Целевая папка".
Выберите пустую папку, куда программа скопирует отсортированные файлы.

---

## 🔁 Шаг 3: Обработка

Нажмите кнопку **"🚀 НАЧАТЬ ОБРАБОТКУ"**.

В процессе работы в окне "Журнал событий" будет отображаться ход выполнения.
Программа выполняет следующие действия:
1.  **Читает файлы**: Извлекает таблицы с отклонениями (Спинка/Корыто).
2.  **Анализирует**: Выявляет "плохие" точки (отклонение < -0.055) и группирует их.
3.  **Принимает решение**: Использует файл `decision_table_T.json` для определения статуса ("Годная", "Группировка", "Брак").
4.  **Копирует**: Переносит файлы в целевую папку, создавая подпапки со статусами (например, `Target/SubFolder/Годная/report.pdf`). Структура исходных папок сохраняется.

---

## 📊 Результат

В целевой папке будут созданы папки с категориями:
- **Годная**: Протоколы без критических отклонений.
- **Группировка**: Протоколы, требующие внимания.
- **Брак**: Протоколы с недопустимыми отклонениями.

В журнале программы отобразится статистика по количеству файлов в каждой категории.

---

## ⚙️ Настройка (decision_table_T.json)

Файл `decision_table_T.json` содержит правила классификации. Вы можете редактировать его для изменения границ допусков.

Пример структуры:
```json
{
  "(0;0.055)": {
    "(0;4)": "Годная",
    "(4;inf)": "Брак"
  }
}
```

---

## 🛑 Решение проблем

- **"Неподдерживаемый формат"**: Программа работает только с PDF и HTML.
- **"Ошибка кодировки"**: Если HTML файл не читается, попробуйте пересохранить его в UTF-8.
- **"Файл занят"**: Закройте файлы, если они открыты в других программах.
