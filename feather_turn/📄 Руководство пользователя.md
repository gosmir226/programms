## Программа: Обработка замеров разворота лопаток

Эта программа обрабатывает Excel-файлы с замерами разворота лопаток, автоматически определяя их тип (Первичный или Перезамер), стандартизируя данные и объединяя всё в единую таблицу.

---

## 🚀 Запуск программы

1.  Запустите программу через `navigation.py` или файл `main_gui.py`.
2.  Нажмите кнопку **"Обзор..."** и выберите корневую папку, в которой лежат папки с измерениями (или сами файлы).

---

## 🛠 Как это работает

Программа сканирует выбранную директорию (включая подпапки) и ищет файлы `.xlsx`.

### 1. Определение типа файла
Программа анализирует имя файла и его структуру:
*   **Первичный**: Файлы, содержащие "угол разворота" в названии или являющиеся единственным файлом с данными. Как правило, имеют 1 лист.
*   **Перезамер**: Файлы, содержащие "перезамер" или "доработка". Также определяются по наличию нескольких листов (каждый лист — отдельный проход).

### 2. Извлечение данных
Из каждого файла извлекаются:
*   **Плавка**: Год и номер плавки (например, из "25В11" -> Год: 25, Номер: 11).
*   **Кассета**: Номер кассеты из названия файла или листа.
*   **Замер**: Значение угла разворота.
*   **Статус**: ГОДН / БРАК / ОТКЛ / ТР.
*   **Коэффициент**: Копируется из первой строки на все остальные.

### 3. Обработка "Брак АТОС"
Если в статусе есть отклонения, программа проверяет дополнительные столбцы ("Замер с поправкой" и др.) на наличие пометки **"АТОС"**. В случае обнаружения:
*   В столбец "Брак АТОС" ставится пометка.
*   Статус пересчитывается на основе скорректированного замера.

### 4. Кэширование
Программа запоминает обработанные файлы (`processing_cache.json`).
*   Если файл не менялся, он пропускается (ускоряет повторный запуск).
*   Если файл изменился, старые данные удаляются, и файл обрабатывается заново.

---

## 📊 Результат

По завершении в папке с исходными файлами создается файл **`consolidated_results.xlsx`**.
В нем собраны все измерения со всех файлов в едином стандартизированном формате.

---

## ❓ Частые вопросы

**В: Программа пишет "Пропуск (не изменился)". Что делать?**
О: Это нормально. Программа не тратит время на файлы, которые вы уже обрабатывали раньше. Если вы хотите пересчитать всё заново, удалите файл `processing_cache.json` в папке.

**В: Файл не попал в отчет.**
О: Проверьте, является ли он `.xlsx` файлом (не `.xls` и не `.csv`). Проверьте, есть ли в нем данные, начинающиеся с заголовков.
