## Программа: Объединение отчетов Термодат

Эта программа предназначена для автоматического объединения (сшивки) разрозненных CSV-отчетов с прибора Термодат в единые файлы, соответствующие полному циклу работы или длительному периоду.

---

## 🚀 Как пользоваться

1.  Запустите программу через `navigation.py` или `main_gui.py`.
2.  В поле **"Входная папка"** выберите корневую директорию, где лежат папки с датами (например, `2023-10-01`, `2023-10-02`...).
3.  В поле **"Выходная папка"** выберите папку, куда будут сохраняться объединенные отчеты.
4.  Нажмите кнопку **"🚀 НАЧАТЬ ОБРАБОТКУ"**.

---

## ⚙️ Алгоритм работы

1.  **Сканирование**: Программа просматривает входную папку, находит подпапки с датами и сортирует их в хронологическом порядке.
2.  **Сшивка цепочек**:
    *   Программа берет файл из первой папки.
    *   Проверяет следующий файл (из следующей папки).
    *   Если разница между *концом* первого файла и *началом* второго файла составляет **60 секунд (±2 сек)**, программа считает их частью одной непрерывной записи.
    *   Файлы объединяются в одну цепочку.
3.  **Сохранение**:
    *   Как только цепочка прерывается (разрыв во времени больше 1 минуты), накопленные данные сохраняются в один файл.
    *   Имя файла: `ДатаНачала-ДатаКонца_merged.csv`.
4.  **Кэширование**:
    *   Обработанные папки запоминаются в скрытом файле `.processing_cache.json` в выходной директории.
    *   При повторном запуске программа пропустит уже обработанные папки, что значительно ускоряет работу.
    *   Кнопка **"🗑 Очистить кэш"** позволяет сбросить память программы и пересчитать всё заново.

---

## 📋 Формат данных

Программа автоматически стандартизирует CSV файлы:
*   Разделитель: `;` (точка с запятой).
*   Формат чисел: с запятой (для Excel).
*   Столбцы: `Дата`, `Время`, `TC405A_Real`, `TC406A_Real`, `AVERAGE_KILN_TEMPERATURE`.

---

## ❓ Решение проблем

**В: Файлы не объединяются, хотя должны.**
О: Проверьте временной разрыв. Алгоритм строго настроен на разрыв в 1 минуту (стандартная дискретность записи прибора). Если прибор был выключен дольше, начнется новая цепочка.

**В: В итоговом файле "каша" из символов.**
О: Программа использует автоопределение кодировки. Убедитесь, что исходные файлы не повреждены.
