## Программа: Обработка данных температур УППФ

Эта программа предназначена для автоматической обработки данных из Excel-отчетов печей УППФ, объединения их с паспортными данными и формирования итогового Excel-файла.

---

## 📦 Установка

Убедитесь, что Python установлен на вашем компьютере.

Для установки необходимых библиотек выполните команду:

```bash
pip install PyQt6 pandas openpyxl xlrd numpy
```

---

## 🚀 Запуск программы

Запустите файл `main_gui.py` из папки `UPPF` или используйте общий навигатор `navigation.py`.  
Появится графическое окно программы.

---

## 📁 Шаг 1: Выбор директории с входными данными

Нажмите кнопку **"Обзор..."** в поле "Папка с данными".

❗ **ВАЖНО: Структура папок**
Выбирайте **КОРНЕВУЮ папку**, в которой лежат папки с плавками.
Внутри каждой папки плавки должны находиться соответствующие Excel-файлы (с листами `ОТЧЕТ`, `ПАСПОРТ`, `ТЕМПЕРАТУРА`).

Пример структуры:
```
📂 Данные_За_ (Выбирать эту папку!)
 ├── 📂 Плавка_123_01.01.2023
 │    └── отчет1.xlsx
 ├── 📂 Плавка_124_02.01.2023
 │    └── отчет2.xlsx
```

Программа автоматически просканирует все подпапки и обработает найденные Excel-файлы.

---

## 📄 Шаг 2: Укажите файл результата

Нажмите **"Выбрать..."** в поле "Файл результата".
Укажите имя и местоположение Excel-файла, куда будут сохранены данные.

- Если файл не существует, он будет создан.
- Если файл существует, новые данные будут добавлены к нему.

---

## 🔁 Шаг 3: Обработка

Нажмите кнопку **"🚀 ЗАПУСТИТЬ ОБРАБОТКУ"**.

В процессе работы в окне "Журнал событий" будет отображаться ход выполнения. 
Программа анализирует температурные кривые, определяет моменты заливки, рассчитывает время выдержки и другие параметры.

---

## 📊 Результат

В итоговом Excel-файле будет создана таблица со следующими данными:
- **Плавка**: Название плавки (из имени папки)
- **Порядок заливки**: Порядковый номер заливки в плавке
- **Образец**: Метка, является ли заливка образцом (по температуре заливки)
- **Технологические параметры**: Температуры (Т2, Т3, Т4), вакуум (PT6), времена выдержки и др.
- **Паспортные данные**: Данные из листа "ПАСПОРТ" (тип печи, даты и т.д.)

---

## 🧠 Кэширование (casheUPPF.json)

Программа запоминает обработанные файлы в `casheUPPF.json` в папке с результирующим файлом (или рядом с программой).
- Повторно запущенная обработка пропустит уже добавленные файлы.
- Если вы хотите пересчитать все заново: удалите `casheUPPF.json` или выберите новый выходной файл.

---

## 🛑 Решение проблем

- **"Не найден столбец ПИРОМЕТР"**: Проверьте, что в файле Excel на листе `ОТЧЕТ` есть столбец с названием `ПИРОМЕТР`.
- **"Ошибка чтения листа"**: Файл Excel может быть поврежден или открыт в другой программе. Закройте Excel перед запуском обработки.
