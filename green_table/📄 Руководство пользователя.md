## Программа: Сбор данных в Зеленую таблицу (Green Table)

Эта программа предназначена для автоматического сбора данных из Excel-файлов (отчетов плавок) и формирования единой сводной таблицы ("Зеленая таблица").

---

## 📦 Установка

Убедитесь, что Python установлен на вашем компьютере.

Для установки необходимых библиотек выполните команду:

```bash
pip install PyQt6 pandas openpyxl
```

---

## 🚀 Запуск программы

Запустите файл `main_gui.py` из папки `green_table` или используйте общий навигатор `navigation.py`.
Появится графическое окно программы с темной темой.

---

## 📁 Шаг 1: Выбор папки с файлами

Нажмите кнопку **"Обзор..."** в поле "Папка с файлами".
Выберите папку, в которой находятся файлы Excel (`.xls` или `.xlsx`) с отчетами о плавках.
Программа сканирует **все вложенные папки**.

---

## 📄 Шаг 2: Выбор выходного файла

Нажмите кнопку **"Обзор..."** в поле "Итоговый Excel файл".
Укажите имя и местоположение для файла, в который будут сохранены результаты (например, `result.xlsx`).

---

## 🔁 Шаг 3: Запуск обработки

Нажмите зеленую кнопку **"🚀 НАЧАТЬ СБОР ДАННЫХ"**.

В окне "Журнал событий" будет отображаться процесс сканирования файлов и обработки каждого листа.
Программа автоматически распознает:
- Печь (УВНК-9А1, УВНК-9А2, УППФ и др.)
- Дату и время
- Номер плавки
- Марку металла и шихты
- Технические условия (ТУ)
- Номера блоков и образцов
- Комплектность оснастки

**Особенности обработки:**
- Группы ячеек с "переборкой" (большие объединенные ячейки с пометкой "перебор") автоматически детектируются.
- Для плавок УВНК автоматически проставляется **"Номер заливки после переборки"**.
- Для всех плавок рассчитывается **"Порядок заливки в плавке"**.

---

## 📊 Результат

По завершении в указанном Excel-файле будут созданы три листа:
1.  **Данные**: Основная сводная таблица со всеми извлеченными параметрами.
2.  **Ошибки**: Список файлов, которые не удалось обработать (если такие есть).
3.  **Статистика**: Общая информация о количестве обработанных файлов и строк.

---

## ⚙️ Настройка (dictionary.json)

Файл `dictionary.json` содержит справочники для распознавания марок металлов и литер плавок.
Если программа не узнает какую-то марку металла, вы можете добавить её в этот файл.

Пример:
```json
{
  "metally": [
    "GTD-111",
    "НовыйСплав-ВИ"
  ]
}
```

---

## 🛑 Решение проблем

- **"Файл занят"**: Убедитесь, что исходные или целевой файлы закрыты в Excel.
- **"Данные не найдены"**: Проверьте, что в выбранной папке есть Excel файлы с правильной структурой (ячейки 3x8).
